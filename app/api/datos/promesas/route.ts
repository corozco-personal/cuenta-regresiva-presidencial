import { campaignPromises } from "../../../data/campaign-promises";
function csv() { const keys = ["id","title","category","status","deadline","progress","assessment","lastReviewed"]; const cell = (v: unknown) => `"${String(v ?? "").replaceAll('"','""')}"`; return [keys.map(cell).join(","), ...campaignPromises.map((p) => keys.map((k) => cell(p[k as keyof typeof p])).join(","))].join("\n"); }
export async function GET(request: Request) { if (new URL(request.url).searchParams.get("format") === "csv") return new Response(csv(), { headers: { "content-type": "text/csv; charset=utf-8", "content-disposition": "attachment; filename=cuenta-publica-promesas.csv" } }); return Response.json({ license: "CC BY 4.0", generatedAt: new Date().toISOString(), count: campaignPromises.length, data: campaignPromises }); }
