"use client";
import { useEffect, useMemo, useState } from "react"; import { Bell, Check, Copy, Rss } from "lucide-react";
const topics=["Todas las noticias","Justicia y control","Seguridad y defensa","Economía","Relaciones exteriores","Elecciones","Derechos","Gobierno"];
export default function AlertPreferences(){const[topic,setTopic]=useState(topics[0]);const[saved,setSaved]=useState(false);const[copied,setCopied]=useState(false);useEffect(()=>{const f=requestAnimationFrame(()=>{const v=localStorage.getItem("cuenta-publica-alert-topic");if(v&&topics.includes(v))setTopic(v)});return()=>cancelAnimationFrame(f)},[]);const feedUrl=useMemo(()=>`/feed.xml${topic===topics[0]?"":`?category=${encodeURIComponent(topic)}`}`,[topic]);function save(){localStorage.setItem("cuenta-publica-alert-topic",topic);setSaved(true);setTimeout(()=>setSaved(false),1800)}async function copyFeed(){await navigator.clipboard.writeText(`${location.origin}${feedUrl}`);setCopied(true);setTimeout(()=>setCopied(false),1800)}return <div className="alerts-grid"><section><Bell size={26}/><p className="section-kicker">PREFERENCIA LOCAL</p><h2>Elige el tema que quieres seguir</h2><p>La selección se guarda solo en tu navegador y crea un enlace RSS filtrado por categoría.</p><label><span>Tema prioritario</span><select value={topic} onChange={e=>setTopic(e.target.value)}>{topics.map(i=><option key={i}>{i}</option>)}</select></label><button onClick={save}>{saved?<Check size={17}/>:<Bell size={17}/>} {saved?"Preferencia guardada":"Guardar en este navegador"}</button></section><section><Rss size={26}/><p className="section-kicker">ALERTA RSS PERSONALIZADA</p><h2>Recibe solo lo que elegiste</h2><p>Agrega el canal a Feedly, Inoreader o cualquier lector RSS. Se actualiza con el monitor cada seis horas.</p><a href={feedUrl} target="_blank">Abrir canal RSS <Rss size={16}/></a><button className="secondary" onClick={copyFeed}>{copied?<Check size={17}/>:<Copy size={17}/>} {copied?"Enlace copiado":"Copiar enlace del canal"}</button></section></div>}
